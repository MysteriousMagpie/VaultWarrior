# Archive

This folder stores deprecated or superseded project components retained for reference.

## ai-vault-planning-cli-1

Archived duplicate of the planning CLI (`ai_vault_cli` package) that previously conflicted with the active `ai-vault-planning-cli/` subproject due to identical package name. The dev script now excludes it from installation and testing. Remove entirely if no longer needed.
